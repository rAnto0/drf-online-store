[project]
name = "frontend"
version = "0.1.0"
description = "Flask frontend for online store"
requires-python = ">=3.11"
dependencies = [
  "Flask==3.1.0",
  "requests==2.32.3",
]

[dependency-groups]
dev = [
  "ruff==0.11.9",
]

[tool.ruff]
line-length = 88
target-version = "py311"
